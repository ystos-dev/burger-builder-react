import React from "react";

const buildControll = (props) => {
    return (
        <div>
            <div>props.label</div>
            <button>Less</button>
            <button>More</button>
        </div>
    );
}

export default buildControll;